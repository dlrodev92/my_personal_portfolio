---
// Meme Section for Index page
const codingBingo = [
  "✅ Blamed the cache",
  "✅ Copied from Stack Overflow", 
  "✅ 'Fixed' it with !important",
  "✅ Turned it off and on again",
  "✅ Works in incognito mode",
  "✅ Forgot to save the file"
];
---

<section class="py-20 px-4 sm:px-6 lg:px-8">
  <div class="max-w-3xl mx-auto text-center">
    
    <!-- Header -->
    <div class="mb-8">
      <h2 class="font-display text-2xl sm:text-3xl font-bold text-black mb-4">
        🤓 You're still reading?
      </h2>
      <p class="font-body text-lg text-text-muted">
        Here's a meme for your troubles:
      </p>
    </div>
    
    <!-- Meme placeholder -->
    <div class="meme-container">
      <div class="bg-surface rounded-2xl p-8 border border-border">
        <div class="space-y-4">
          <div class="text-6xl">😂</div>
          <div class="font-body text-text-secondary">
            <p class="mb-2 font-medium">"It works on my machine"</p>
            <p class="text-sm text-text-muted">
              - Every developer ever, just before production breaks
            </p>
          </div>
        </div>
      </div>
    </div>
    
    <!-- Developer Bingo bonus -->
    <div class="mt-12">
      <h3 class="font-display text-lg font-bold text-black mb-4">
        Developer Bingo 🎯
      </h3>
      <div class="grid grid-cols-2 sm:grid-cols-3 gap-3 text-sm">
        {codingBingo.map((item, index) => (
          <div class="bg-surface/50 rounded-lg p-3 border border-border font-body text-text-muted">
            {item}
          </div>
        ))}
      </div>
    </div>
  </div>
</section>
