---
// Hero Section for Index page
---
<section class="relative min-h-screen bg-white overflow-hidden flex items-center justify-start ">
  <!-- Background image -->
  <div
    class="absolute inset-0 w-full h-full bg-center bg-cover animate-slow-zoom"
    style="background-image: url('/BM.webp');"
  ></div>

  <!-- Triangle overlay -->
  <div
    class="absolute inset-0 w-full h-full bg-text-secondary/85 clip-triangle"
  ></div>

  <!-- Hero Text (uses flex now, no relative/pt-xx) -->
  <div class="z-20 flex flex-col items-start max-w-xl px-6 text-white mb-20 ml-10">
    <h1 class="text-5xl sm:text-7xl font-display font-extrabold uppercase leading-none">
      I code <span class="text-yellow-400">ridiculous</span> shit
    </h1>
    <p class="mt-4 text-white/80 text-xl sm:text-2xl">
      Based in Granada & London — full-stack builder, cloud hustler, indie hacker.
    </p>
  </div>

  <!-- LONDON -->
  <div class="absolute z-20 flex items-center gap-2 animate-dots font-display
    top-[28%] left-[24%]
    sm:top-[30%] sm:left-[26%]
    min-[700px]:top-[29%] min-[700px]:left-[31%]
    md:top-[29%] md:left-[32%]
    min-[880px]:top-[28%] min-[880px]:left-[36%]
    lg:top-[25%] lg:left-[37%]
    min-[1150px]:top-[29%] min-[1150px]:left-[38%]
    xl:top-[22%] xl:left-[40%]
  ">
    <div class="w-4 h-4 md:w-6 md:h-6 bg-red-500 rounded-full animate-pulse border-2 border-white"></div>
    <span class="text-sm text-white font-medium flex items-center gap-1">
      🇬🇧 <span>London</span>
    </span>
  </div>

  <!-- GRANADA -->
  <div class="absolute z-20 flex items-center gap-2 animate-dots font-display
    top-[40%] left-[23%]
    sm:top-[40%] sm:left-[26%]
    min-[700px]:top-[40%] min-[700px]:left-[30%]
    md:top-[41%] md:left-[32%]
    min-[880px]:top-[40%] min-[880px]:left-[35%]
    lg:top-[40%] lg:left-[37%]
    min-[1150px]:top-[40%] min-[1150px]:left-[39%]
    xl:top-[38%] xl:left-[40%]
  ">
    <div class="w-4 h-4 md:w-6 md:h-6 bg-yellow-400 rounded-full animate-pulse border-2 border-white"></div>
    <span class="text-sm text-white font-medium flex items-center gap-1">
      🇪🇸 <span>Granada</span>
    </span>
  </div>
</section>
